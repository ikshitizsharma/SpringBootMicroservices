spring:  application:    name: DEPARTMENT-SERVICE

server:
  port: 9001  #to register itself to the resgitsry server  eureka:  client:    fetch-registry: true    register-with-eureka: true    service-url:      defaultZone: http://localhost:8761/eureka/  instance:    hostname: localhost